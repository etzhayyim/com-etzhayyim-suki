{:paths ["src" "test"]
 :tasks
 {test {:doc "Run suki cljc actor boundary tests"
        :task (do
                (require 'clojure.test
                         'suki.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'suki.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
